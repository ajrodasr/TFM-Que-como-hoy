import { CambiarPasswordPerfil } from './cambiar-password-perfil';

describe('CambiarPasswordPerfil', () => {
  it('should create an instance', () => {
    expect(new CambiarPasswordPerfil()).toBeTruthy();
  });
});
