import { GrupoIngredienteConsumido } from './grupo-ingrediente-consumido';

describe('GrupoIngredienteConsumido', () => {
  it('should create an instance', () => {
    expect(new GrupoIngredienteConsumido()).toBeTruthy();
  });
});
